name: Terraform CI/CD

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::098891530178:role/github-actions-role
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Terraform Init
        run: terraform init -input=false

      - name: Terraform Plan (JSON)
        if: github.event_name == 'pull_request'
        run: |
          terraform plan 
          terraform plan -json > plan.json

      - name: Generate Plan Summary
        if: github.event_name == 'pull_request'
        run: |
          jq -r '
          select(.type == "planned_change") |
          "- Name:\(.change.resource.resource_name) - \(.change.resource.addr): \(.change.action)"
          ' plan.json > summary.txt

      - name: Comment PR with Summary
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            let summary = '';
            try {
              summary = fs.readFileSync('summary.txt', 'utf8').trim();
            } catch (e) {
              summary = '';
            }

            const body = summary
              ? `## Terraform Change Summary 🚀\n\n${summary}`
              : `## Terraform Change Summary 🚀\n\nNo infrastructure changes detected.`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });

      - name: Terraform Apply
        if: github.event_name == 'push'
        run: terraform apply -auto-approve
